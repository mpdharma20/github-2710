pipeline {
    agent any

    stages {
        stage('IP Address INFO') {
            steps {
                sh "hostname -I"
            }
        }

        stage('CPU Info') {
            steps {
                sh "lscpu"
            }
        }
    }
}
